# Required for JWT session signing (use a long random string in production).
AUTH_SECRET=

# Optional; inferred in dev when unset.
# AUTH_URL=http://localhost:3000

# Demo credentials for the credentials provider until the Fastify auth API exists.
DEMO_USER_EMAIL=demo@example.com
DEMO_USER_PASSWORD=change-me-in-production
