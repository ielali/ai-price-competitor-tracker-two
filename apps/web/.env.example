# Required in production (minimum 32 characters). Used to sign JWT access and refresh tokens.
AUTH_SECRET=

# Optional: seed an in-memory demo account for local development (no database until API ships).
# Legacy aliases AUTH_DEMO_USER_EMAIL / AUTH_DEMO_PASSWORD are also supported.
DEMO_USER_EMAIL=
DEMO_USER_PASSWORD=
# DEMO_USER_NAME=
