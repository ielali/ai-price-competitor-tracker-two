# Required for production and for `next build` after NextAuth is enabled.
AUTH_SECRET=

# Canonical URL of the app (optional locally).
AUTH_URL=http://localhost:3000

# Demo credentials provider — replace in production with API-backed auth.
DEMO_USER_EMAIL=demo@example.com
DEMO_USER_PASSWORD=changeme
